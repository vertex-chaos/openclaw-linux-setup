name: lint
on:
  push:
  pull_request:
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install lint dependencies
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends make shellcheck
      - name: Run repo lint target
        run: make lint
